{
  "name": "task-app",
  "version": "1.0.0",
  "main": "index.js",
  "scripts": {
    "infra:up": "docker compose up -d",
    "infra:down": "docker compose down",
    "infra:wait": "wait-on tcp:localhost:5432 tcp:localhost:9092 tcp:localhost:6380 -t 120000",
    "dev:terminals": "bash scripts/dev-terminals.sh",
    "dev:concurrent": "concurrently -k -n gateway,auth,task,email,notif,web -c blue,green,yellow,magenta,cyan,white \"npm run dev --prefix taskflow-backend/api-gateway\" \"npm run dev --prefix taskflow-backend/auth-service\" \"npm run dev --prefix taskflow-backend/task-service\" \"npm run dev --prefix taskflow-backend/email-service\" \"npm run dev --prefix taskflow-backend/notification-service\" \"npm run dev --prefix taskflow-frontend\"",
    "dev": "npm run infra:up && npm run infra:wait && npm run dev:terminals"
  },
  "devDependencies": {
    "concurrently": "^9.2.1",
    "wait-on": "^8.0.5"
  },
  "keywords": [],
  "author": "",
  "license": "ISC",
  "description": ""
}
