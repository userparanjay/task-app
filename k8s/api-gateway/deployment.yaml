apiVersion: apps/v1
kind: Deployment
metadata:
  name: api-gateway
  namespace: taskflow

spec:
  replicas: 1

  selector:
    matchLabels:
      app: api-gateway

  template:
    metadata:
      labels:
        app: api-gateway

    spec:
      containers:
        - name: api-gateway
          image: pnajan/taskflow-api-gateway:v1

          ports:
            - containerPort: 5000

          envFrom:
            - configMapRef:
                name: api-gateway-config