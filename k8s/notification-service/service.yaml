apiVersion: v1
kind: Service
metadata:
  name: notification-service
  namespace: taskflow
  labels:
    app: notification-service
spec:
  type: ClusterIP
  selector:
    app: notification-service
  ports:
    - name: http
      port: 5005
      targetPort: 5005
