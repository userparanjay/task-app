apiVersion: v1
kind: Service
metadata:
  name: task-service
  namespace: taskflow

spec:
  selector:
    app: task-service

  ports:
    - protocol: TCP
      port: 5004
      targetPort: 5004

  type: ClusterIP